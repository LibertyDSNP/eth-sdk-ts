// import { Registration } from "../core/contracts/registry";
// import { EthereumAddress } from "./types/Strings";

/**
 * findRegistrations() takes a DSNP message and returns a serialized string.
 *
 * @param address - A public address to lookup
 * @returns A promise with containing a list registrations for public address
 */
// export const findRegistrations = (address: EthereumAddress): Promise<Registration[]> => {};
