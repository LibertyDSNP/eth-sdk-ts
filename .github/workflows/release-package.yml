name: Release Package
on:
  release:
    types: [ released ]
jobs:
  publish-to-npm:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        node-version: [14.6.0]
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install 💾
        run: npm ci

      - name: Publish 🚂
        run: npm publish --new-version ${{ github.event.release.tag_name }} --tag latest --debug --dry-run
        env:
          NODE_AUTH_TOKEN: ${{secrets.NODE_AUTH_TOKEN}}
